import React from "react";
import { Box, Button } from "@mui/material";

export default function FriendSearch() {
  return (
    <div>
        <Button onClick={console.log("Hey!")}>This</Button>
    </div>
  );
}
